//REPL

/*
	node 						type to start using Node in the console.
	_  							saved for the result of the last operation.
	.clear  					cancels operation.
	ctrl + c  					cancels operation.
	.break    					cancels operation.
	.save       				cancels operation.
	contrl + d 					exits the node REPL
	.exit   					exits de node REPL
	.save hi.js 				saves session in a file.
	.load hi.js 				loads the saved session.
	.help    					shows all the commands available in the REPL.
	exports.fileName = blah		makes that file available on every program taht requires this file. 

*/